<?xml version="1.0" encoding="utf-8"?>
<!-- Meta Quest VR manifest — immersive OpenXR application -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.rp1.Rubidium">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Quest requires Vulkan -->
    <uses-feature android:name="android.hardware.vulkan.level" android:version="1" android:required="true" />

    <!-- Mark as VR application -->
    <uses-feature android:name="oculus.software.handtracking" android:required="false" />
    <uses-feature android:name="android.hardware.vr.headtracking" android:version="1" android:required="true" />

    <application
        android:label="Rubidium"
        android:icon="@mipmap/ic_launcher"
        android:hasCode="true"
        android:allowBackup="false"
        android:resizeableActivity="false">

        <!-- Required for Quest store -->
        <meta-data android:name="com.oculus.supportedDevices" android:value="quest3|questpro|quest2" />
        <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />

        <activity
            android:name="org.libsdl.app.SDLActivity"
            android:exported="true"
            android:configChanges="density|keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|uiMode"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:launchMode="singleTask"
            android:excludeFromRecents="false">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- Quest VR intent -->
                <category android:name="com.oculus.intent.category.VR" />
            </intent-filter>

            <meta-data android:name="SDL_MAIN_ENTRY_POINT" android:value="main" />
        </activity>
    </application>
</manifest>
