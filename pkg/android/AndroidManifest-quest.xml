<?xml version="1.0" encoding="utf-8"?>
<!-- Meta Quest VR manifest — immersive OpenXR application -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.rp1.Rubidium">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Quest requires Vulkan -->
    <uses-feature android:name="android.hardware.vulkan.level" android:version="1" android:required="true" />
    <uses-feature android:name="android.hardware.vr.headtracking" android:version="1" android:required="true" />
    <uses-feature android:name="oculus.software.handtracking" android:required="false" />

    <application
        android:label="Rubidium"
        android:icon="@mipmap/ic_launcher"
        android:hasCode="true"
        android:allowBackup="false"
        android:resizeableActivity="false">

        <meta-data android:name="com.oculus.supportedDevices" android:value="quest3|quest3s|questpro|quest2" />
        <meta-data android:name="com.oculus.vr.focusaware" android:value="true" />

        <!-- MainActivity preloads SDL3 / ANARI / Halogen (stock SDLActivity would skip Halogen).
             The EditText is a hidden IME target for the Horizon system keyboard, not 2D chrome. -->
        <activity
            android:name="com.rp1.Rubidium.MainActivity"
            android:exported="true"
            android:configChanges="density|keyboard|keyboardHidden|navigation|orientation|screenLayout|screenSize|uiMode"
            android:screenOrientation="landscape"
            android:theme="@android:style/Theme.Black.NoTitleBar.Fullscreen"
            android:launchMode="singleTask"
            android:excludeFromRecents="false">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="com.oculus.intent.category.VR" />
            </intent-filter>

            <meta-data android:name="SDL_MAIN_ENTRY_POINT" android:value="main" />
        </activity>
    </application>
</manifest>
